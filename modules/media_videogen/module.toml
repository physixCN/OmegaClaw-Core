id = "omegaclaw.media.videogen"
kind = "media-organ"
version = "0.1.0"
entrypoint = "entry.metta"
catalog = "catalog.metta"
signatures = "signatures.metta"
optional = true
default_enabled = true

provides = [
  "skill:generate-video",
  "skill:video-config-status",
]

requires = [
  "python>=3.10",
]

[trace]
writes = [
  "VideoGenerated",
  "VideoConfigObserved",
]

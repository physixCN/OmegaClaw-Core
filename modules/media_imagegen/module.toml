id = "omegaclaw.media.imagegen"
kind = "media-organ"
version = "0.1.0"
entrypoint = "entry.metta"
catalog = "catalog.metta"
signatures = "signatures.metta"
optional = true
default_enabled = true

provides = [
  "skill:generate-image",
  "skill:generate-image-quality",
]

requires = [
  "python>=3.10",
]

[trace]
writes = [
  "ImageGenerated",
]

id = "omegaclaw.sense.audio"
kind = "sense"
version = "0.1.0"
entrypoint = "entry.metta"
catalog = "catalog.metta"
signatures = "signatures.metta"
optional = true
default_enabled = true

provides = [
  "sense:audio",
  "skill:observe-audio",
  "skill:inspect-audio",
  "skill:audio-observation-log",
]

requires = [
  "python>=3.10",
]

[trace]
writes = [
  "AudioObserved",
  "AudioInspection",
]

id = "omegaclaw.channel.web-control"
kind = "channel"
version = "0.1.0"
entrypoint = "entry.metta"
catalog = "catalog.metta"
signatures = "signatures.metta"
optional = true
default_enabled = true

provides = [
  "channel:web-control",
  "skill:send-web-control",
  "skill:send-web-control-base64",
]

requires = [
  "python>=3.10",
]

[trace]
writes = [
  "WebControlMessageReceived",
  "WebControlMessageSent",
]

id = "openclaw.surface.operator-console"
kind = "operating-surface"
version = "0.1.0"
entrypoint = "entry.metta"
optional = true

provides = [
  "surface:operator-console",
  "skill:open-window",
  "skill:show-artifact",
  "skill:move-window",
  "skill:focus-surface",
]

requires = [
  "browser:any",
  "websocket:any",
]

[trace]
writes = [
  "SurfaceEvent",
  "OperatorInteraction",
  "SurfaceStateChanged",
]

diff --git a/run.metta b/run.metta
index 2ebde23..5a6976a 100644
--- a/run.metta
+++ b/run.metta
@@ -1,5 +1,8 @@
 !(import! &self (library lib_import))
 !(git-import! "https://github.com/asi-alliance/OmegaClaw-Core.git")
-!(import! &self (library OmegaClaw-Core lib_omegaclaw))
+!(import! &self (car-atom (collapse (library OmegaClaw-Core lib_omegaclaw_no_agentverse))))
+!(import! &self (car-atom (collapse (library OmegaClaw-Core lib_omegaclaw_assume))))
+!(import! &self (car-atom (collapse (library OmegaClaw-Core lib_omegaclaw_attention))))
+!(import! &self (car-atom (collapse (library OmegaClaw-Core lib_omegaclaw_body))))
 
 !(omegaclaw)
diff --git a/lib_omegaclaw_no_agentverse.metta b/lib_omegaclaw_no_agentverse.metta
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/lib_omegaclaw_no_agentverse.metta
@@ -0,0 +1,46 @@
+; Local Omega composition: core substrate without the historical Agentverse POC.
+; Agentverse now loads as a normal optional module instead of core-skill glue.
+
+!(import! &self (library lib_patrick))
+!(import! &self (library lib_llm))
+!(import! &self (library lib_vector))
+!(import! &self (library lib_combinatorics))
+!(import! &self (library lib_spaces))
+!(import! &self (library OmegaClaw-Core lib_nal))
+!(import! &self (library OmegaClaw-Core lib_pln))
+!(import! &self (library lib_nars))
+!(import! &self (library OmegaClaw-Core lib_llm_ext.py))
+!(import! &self (library OmegaClaw-Core ./src/helper.py))
+!(import! &self (library OmegaClaw-Core ./channels/irc.py))
+!(import! &self (library OmegaClaw-Core ./channels/mattermost.py))
+!(import! &self (library OmegaClaw-Core ./channels/telegram.py))
+!(import! &self (library OmegaClaw-Core ./channels/slack.py))
+!(import! &self (library OmegaClaw-Core ./channels/websearch.py))
+!(import! &self (library OmegaClaw-Core ./src/utils))
+!(import! &self (library OmegaClaw-Core ./src/channels))
+!(import! &self (library OmegaClaw-Core ./src/energy.py))
+!(import! &self (library OmegaClaw-Core ./src/skill_catalog.metta))
+!(import! &self (library OmegaClaw-Core ./src/skill_catalog_core.metta))
+!(import! &self (library OmegaClaw-Core ./src/skill_catalog_memory.metta))
+!(import! &self (library OmegaClaw-Core ./src/skill_catalog_energy.metta))
+!(import! &self (library OmegaClaw-Core ./src/skill_catalog_reasoning.metta))
+!(import! &self (library OmegaClaw-Core ./src/skill_catalog_affordance.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_core.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_runtime_spaces.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_memory.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_energy.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_reasoning_spaces.metta))
+!(import! &self (library OmegaClaw-Core ./modules/scratch_space/entry.metta))
+!(import! &self (library OmegaClaw-Core ./modules/agentverse/entry.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_affordance.metta))
+!(import! &self (library OmegaClaw-Core ./src/skills_space_mutation.metta))
+!(import! &self (library OmegaClaw-Core ./src/memory))
+!(import! &self (library OmegaClaw-Core ./src/loop))
+!(git-import! "https://github.com/patham9/petta_lib_chromadb.git")
+!(import! &self (library petta_lib_chromadb lib_chromadb))
+!(bind! &persistent (new-space))
+!(bind! &agenda (new-space))
+!(bind! &beliefs (new-space))
+!(bind! &world (new-space))
+!(bind! &events (new-space))
+!(bind! &activity (new-space))
diff --git a/src/skills_core_no_agentverse.metta b/src/skills_core_no_agentverse.metta
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/src/skills_core_no_agentverse.metta
@@ -0,0 +1,65 @@
+(= (read-file $file)
+   (progn (translatePredicate (exists_file $file))
+          (translatePredicate (read_file_to_string $file $content ()))
+          $content))
+
+(= (write-file $file $str)
+   (progn (translatePredicate (open $file write $Out))
+          (translatePredicate (write $Out $str))
+          (translatePredicate (close $Out))
+          WRITE-FILE-SUCCESS))
+
+(= (append-file $file $str)
+   (progn (translatePredicate (exists_file $file))
+          (translatePredicate (open $file append $Out))
+          (translatePredicate (write $Out $str))
+          (translatePredicate (nl $Out)),
+          (translatePredicate (close $Out))
+          APPEND-FILE-SUCCESS))
+
+(= (write-file-base64 $file $payload)
+   (py-call (helper.write_file_base64 $file $payload)))
+
+(= (append-file-base64 $file $payload)
+   (py-call (helper.append_file_base64 $file $payload)))
+
+(= (current-swipl-pid)
+   (py-call (helper.current_swipl_pid)))
+
+(= (body-status)
+   (py-str ("BODY-STATUS swipl_pid=" (current-swipl-pid))))
+
+(= (prepare-reboot $reason)
+   (let $trace (py-call (helper.prepare_reboot $reason))
+        (progn (remember $trace)
+               $trace)))
+
+(= (complete-reboot-check)
+   (py-call (helper.complete_reboot_check)))
+
+(= (restart-omega $reason)
+   (py-call (helper.restart_omega $reason)))
+
+(= (wait $reason)
+   (py-str (WAITING: $reason)))
+
+(= (episodes-at $time)
+   (py-call (helper.episodes_at $time (maxEpisodeRecallLines))))
+
+(= (test-metta $expr)
+   (py-call (helper.test_metta_expression $expr)))
+
+(= (run-metta-file $file)
+   (py-call (helper.run_metta_file $file)))
+
+(= (trace-atom $action $space $atom $reason)
+   (add-atom &activity (AtomTouch (get-state &cycle) $action $space $atom $reason (get_time_as_string))))
+
+(= (trace-space $action $space $reason)
+   (add-atom &activity (SpaceTouch (get-state &cycle) $action $space $reason (get_time_as_string))))
+
+(= (trace-merge $source $target $result $reason)
+   (add-atom &activity (AtomMerge (get-state &cycle) $source $target $result $reason (get_time_as_string))))
+
+(= (activity-traces)
+   (repr (get-atoms &activity)))
